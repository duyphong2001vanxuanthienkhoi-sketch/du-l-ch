# Currency Symbol
NEXT_PUBLIC_CURRENCY_SYMBOL = '$'

# Chuỗi bí mật ký phiên đăng nhập (đặt chuỗi ngẫu nhiên dài khi chạy thật)
AUTH_SECRET = 'doi-thanh-chuoi-ngau-nhien-cua-ban'

# Pusher Channels — chat realtime khách ↔ quán (lấy trong dashboard Pusher > App Keys)
PUSHER_APP_ID = ''
PUSHER_KEY = ''
PUSHER_SECRET = ''
PUSHER_CLUSTER = 'ap1'
# Bản công khai (client) — trùng key/cluster ở trên
NEXT_PUBLIC_PUSHER_KEY = ''
NEXT_PUBLIC_PUSHER_CLUSTER = 'ap1'

# Web Push — thông báo đẩy cả khi đóng app. Tạo khóa: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY = ''
VAPID_PRIVATE_KEY = ''
VAPID_SUBJECT = 'mailto:ban@example.com'
NEXT_PUBLIC_VAPID_PUBLIC_KEY = ''
